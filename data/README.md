# data

Папка для датасета не хранится в git.

Ожидаемая структура:

```text
raw/
  videos/
  frames/
processed/
  body_parts_yolo/
    images/train
    images/val
    labels/train
    labels/val
    data.yaml
```
